public class Main {
    public static void main(String[] args) {
//        6. Directly access the method by class name because the display() is static
//          Collage.display();

//        7. Directly access the method by class name because the display() is static
//          Collage.display();
        //        8. Directly access the method by class name because the display() is static
        Collage.display();

    }
}